[spoiler]
# 🔐 מדריך מנהל – עויזר צ'אט

> מסמך זה מיועד **למנהלים בלבד** ומכיל את כל יכולות הניהול, הגדרות הסביבה, ופירוט מלא של שלוחת הניהול.

---

## 🏗️ ארכיטקטורת המערכת

המערכת מורכבת מהמודולים הבאים:

| קובץ | תפקיד |
|------|--------|
| `api/index.js` | ליבת המערכת – State Machine, IVR Router, כל לוגיקת השיחות |
| `api/voice-engine.js` | מנוע TTS נוירלי (אברי/הילה) דרך Microsoft Edge |
| `api/TrebloManager.js` | יצירת שירים דרך Treblo/Sonauto API |
| `api/code-agent-manager.js` | עויזר קוד – AI agent לפיתוח דרך GitHub/Vercel |
| `api/mcp-manager.js` | שרתי MCP (מזג אוויר, תחבורה, מסלולים, עסקים) |
| `api/web-admin.js` | ממשק ניהול האינטרנט – דפי HTML לניהול מפתחות |

**מחסנית טכנולוגית:**
- Node.js על Vercel Serverless Functions
- Upstash Redis – אחסון נתונים מתמיד (profiles, stats, shared chats, notice board)
- Gemini API (google) – מוח הבינה המלאכותית
- Tavily API – חיפוש אינטרנט אמיתי
- call2all / ימות המשיח – מנוע IVR הטלפוני

---

## ⚙️ משתני סביבה נדרשים (Vercel Environment Variables)

| משתנה | תפקיד | חובה? |
|--------|--------|-------|
| `GEMINI_KEYS` | מפתחות Gemini API מופרדים בפסיק | כן |
| `CALL2ALL_TOKEN` | טוקן ימות המשיח (call2all) | כן |
| `UPSTASH_REDIS_REST_URL` | כתובת Upstash Redis | כן (לשמירת נתונים) |
| `UPSTASH_REDIS_REST_TOKEN` | טוקן Upstash Redis | כן (לשמירת נתונים) |
| `ADMIN_PASSWORD` | סיסמת ניהול לשלוחת הניהול | כן |
| `ADMIN_EMAIL` | כתובת מייל לקבלת התראות ניהוליות | מומלץ |
| `TAVILY_API_KEY` | מפתח Tavily לחיפוש אינטרנט אמיתי | מומלץ |
| `TREBLO_API_KEY` | מפתח Treblo ליצירת שירים | אופציונלי |
| `OIZER_MAIL_SCRIPT_URL` | כתובת Google Apps Script לשליחת מיילים | אופציונלי |
| `OIZER_MAIL_SECRET` | מפתח אבטחה לשרות המייל | אופציונלי |
| `PUBLIC_BASE_URL` | כתובת הבסיס של הפרויקט בוורסל | מומלץ |

> **הערה:** ללא Redis, המערכת פועלת במצב זיכרון נדיף בלבד – נתוני משתמשים לא ישמרו בין שיחות!

---

## 🌐 ממשק ניהול אינטרנט

### דף ניהול מפתחות API (ציבורי)
**כתובת:** `https://[domain]/api?web=admin`

דף זה מאפשר למשתמשים:
- הצגה וניהול מפתחות ה-API האישיים שלהם
- תרומת מפתח לציבור (עם אפשרות הגבלת טוקנים)
- ניהול מפתחות שתרמו (מחיקה, עדכון הגבלת טוקנים)

### דף ניהול מערכת (מנהל)
**כתובת:** `https://[domain]/api?web=system_admin`

**דרישה:** התחברות עם `ADMIN_PASSWORD`

דף זה מציג:
- סטטיסטיקות מערכת (מספר שיחות, הצלחות, שגיאות, משתמשים ייחודיים)
- רשימת כל המשתמשים עם: מספר שיחות, כתובת מייל שמורה, מספר מפתחות
- יכולת חסימה/שחרור משתמשים
- היסטוריית שיחות של כל משתמש

---

## 📞 שלוחת הניהול – מדריך מלא

### כניסה לאזור הניהול

מהתפריט הראשי, לחצו **9**.

**עבור מנהל בעל מספר הטלפון המוגדר בקוד (`adminBypassPhone`):**
זיהוי אוטומטי – כניסה ישירה לתפריט הניהול ללא סיסמה.

**לכולם:**
המערכת תבקש את **סיסמת הניהול** (8 ספרות) ולחיצת **#**.
- סיסמה שגויה → חזרה לתפריט הראשי.

---

### 📊 תפריט ניהול

| לחיצה | פעולה |
|--------|--------|
| **1** | נתוני מערכת (סטטיסטיקות) |
| **2** | ניהול משתמש לפי מספר טלפון |
| **3** | רשימת משתמשים (דפדוף בכל המשתמשים) |
| **4** | סטטוס מפתחות API |
| **7** | עויזר קוד (AI לפיתוח) |
| **8** | תפריט API ויצירת שירים |
| **9** | הוספה לרשימה הלבנה |
| **0** | חזרה לתפריט הראשי |

---

#### 📈 נתוני מערכת (לחיצה 1)

המערכת מקריאה:
- סך כל השיחות שנפתחו
- מספר תשובות מוצלחות
- מספר שגיאות
- מספר משתמשים ייחודיים

חוזר לתפריט הניהול אוטומטית.

---

#### 👤 ניהול משתמש לפי מספר טלפון (לחיצה 2)

1. המערכת מבקשת מספר טלפון (10 ספרות) + **#**
2. המערכת מאשרת את המספר ומציגה אפשרויות:

| לחיצה | פעולה |
|--------|--------|
| **1** | חסימת המשתמש לצמיתות |
| **2** | שחרור מחסימה |
| **3** | מחיקת כל נתוני המשתמש |
| **0** | חזרה לתפריט הניהול |

> **אזהרה:** מחיקת נתוני משתמש (לחיצה 3) בלתי הפיכה – מוחקת את כל היסטוריית השיחות, הגדרות, ומפתחות האישיים.

---

#### 📋 רשימת משתמשים (לחיצה 3)

דפדוף בין כל המשתמשים שהשתמשו במערכת. עבור כל משתמש:

| לחיצה | פעולה |
|--------|--------|
| **1** | ניהול המשתמש הנוכחי (חסימה/שחרור/מחיקה) |
| **2** | מעבר למשתמש הבא ברשימה |
| **3** | חיוג חינמי למשתמש (ניתוביה) |
| **0** | חזרה לתפריט הניהול |

בסיום הרשימה – המערכת מודיעה "סוף רשימת המשתמשים".

---

#### 🔑 סטטוס מפתחות API (לחיצה 4)

המערכת מקריאה עבור כל מפתח Gemini API במאגר:
- 4 ספרות אחרונות של המפתח
- מצב: **פעיל** / **חסום**
- מספר שימושים כולל
- שעות עד שחרור החסימה (אם חסום)

> **הסבר מנגנון החסימה:** כאשר מפתח מגיע למגבלה יומית (quota), הוא נחסם ל-24 שעות. מגבלת קצב זמנית (rate limit) נחסמת ל-2 דקות בלבד.

---

#### ➕ הוספה לרשימה הלבנה (לחיצה 9)

מאפשר הוספת מספר טלפון לרשימת ה-`WhiteList.ini` של ימות המשיח.

1. הקישו את מספר הטלפון להוספה + **#**
2. המערכת מנסה להוסיף לקובץ דרך ה-API של call2all
3. הודעת הצלחה / כישלון

---

### 🎵 תפריט API ויצירת שירים (לחיצה 8)

#### תפריט API הראשי

| לחיצה | פעולה |
|--------|--------|
| **1** | יצירת שיר חדש |
| **2** | הגדרות API (מידע בלבד) |
| **0** | חזרה לתפריט הניהול |

#### יצירת שיר חדש (לחיצה 1)

> דורש הגדרת `TREBLO_API_KEY` בסביבת הפרויקט.

**תהליך יצירת שיר:**
1. המערכת מבקשת הקלטה – **תארו את השיר הרצוי** (נושא, סגנון, מילים רצויות)
2. לחצו **#** לסיום ההקלטה
3. Gemini מתמלל ויוצר מפרט מוזיקלי מקצועי (מילות שיר בעברית + תיאור סגנון באנגלית + תגיות)
4. Treblo מייצר את השיר (עלול לקחת עד 45 שניות)
5. השיר נשמר אוטומטית בשלוחה 800 של ימות המשיח
6. המערכת מודיעה: מספר שלוחה ומספר קובץ

**אם היצירה אורכת זמן רב:**
- המערכת תציג: "לבדיקה שוב הקישו כל מקש. לביטול הקישו \*"
- ניתן לבדוק שוב מספר פעמים

#### הגדרות API (לחיצה 2)
מידע בלבד – מפתח Treblo מנוהל דרך משתני הסביבה של וורסל ולא ניתן לשינוי מכאן.

---

### 💻 עויזר קוד (לחיצה 7)

עויזר קוד הוא סוכן AI שמאפשר פיתוח ותיקון קוד **דרך הטלפון**, ללא גישה למחשב.

> **דורש הגדרה:** משתני סביבה של GitHub ו-Vercel (מפורטים בקובץ `code-agent-manager.js`).

#### תפריט עויזר קוד

| לחיצה | פעולה |
|--------|--------|
| **1** | מתן הוראת פיתוח חדשה |
| **2** | בדיקת מצב סביבת הפיתוח |
| **3** | אישור מיזוג השינויים לפרודקשן |
| **4** | ביטול כל השינויים שלא אושרו |
| **0** | חזרה לתפריט הניהול |

#### מתן הוראת פיתוח (לחיצה 1)
1. הקליטו את ההוראה לעויזר קוד (תארו מה לבדוק, לתקן, או להוסיף)
2. לחצו **#** לסיום
3. המערכת מתמללת ומעבירה ל-Gemini + GitHub
4. עויזר קוד מבצע את השינויים בסביבת הפיתוח (branch `dev` בלבד, לא `main`)
5. המערכת מקריאה סיכום של הפעולות שבוצעו

#### בדיקת מצב (לחיצה 2)
- אם אין שינויים – "כרגע אין שינויים ממתינים"
- אם יש שינויים – מספר הקבצים שהשתנו + שליחת קישור לסביבת הבדיקה למייל המנהל

#### אישור מיזוג לפרודקשן (לחיצה 3)
**תהליך בטיחות דו-שלבי:**

**שלב א':**
- המערכת מציגה "נמצאו שינויים. לפתיחת בקשת מיזוג הקישו 1. לביטול הקישו 0"
- לחיצת **1** → פותחת Pull Request ב-GitHub
- קישור ה-PR נשלח למייל המנהל

**שלב ב':**
- "לאישור סופי ומיזוג לפרודקשן הקישו 1. לביטול הקישו 0"
- ⚠️ **לחיצת 1 מבצעת מיזוג אמיתי לפרודקשן!** וורסל מפרס אוטומטית תוך דקות.

#### ביטול שינויים (לחיצה 4)
- "לביטול ומחיקת כל השינויים הלא מאושרים הקישו 1. לביטול הקישו 0"
- מאפס את branch `dev` לתואם `main` הנוכחי

---

## 📊 ניהול מפתחות Gemini – מנגנון מתקדם

### הקצאה אוטומטית למשתמשים חדשים
כל משתמש חדש מקבל אוטומטית **4 מפתחות** מתוך מאגר המפתחות המשותף. מפתחות שהוקצו למשתמש ספציפי לא משמשים יותר בסבב הכללי.

### מאגר מפתחות
מורכב מ:
1. מפתחות שב-`GEMINI_KEYS` (משתנה סביבה)
2. מפתחות שנתרמו על ידי משתמשים (דרך דף ניהול המפתחות)

### סבב מפתחות חכם (Smart Round-Robin)
- מפתח שהגיע למגבלה יומית → חסום 24 שעות
- מפתח עם rate limit זמני → חסום 2 דקות בלבד
- המערכת עוברת אוטומטית למפתח הבא הזמין

### הגבלת טוקנים למפתחות
ניתן להגביל את מספר הטוקנים המקסימלי שמפתח יכול להשתמש בבקשה אחת – עבור מפתחות אישיים ועבור מפתחות שנתרמו.

---

## 📁 אחסון נתונים ב-Redis

| מפתח Redis | תוכן |
|------------|-------|
| `user_profile:{phone}` | פרופיל מלא של משתמש |
| `global_system_stats` | סטטיסטיקות מערכת גלובליות |
| `global_notice_board` | מודעות לוח המודעות (עד 30) |
| `shared_chat:{code}` | שיחה משותפת (תוקף 30 יום) |
| `user_shares:{phone}` | רשימת קודי שיחות משותפות למשתמש |
| `gemini_assigned_keys` | מפתחות שהוקצו למשתמשים ספציפיים |
| `gemini_donated_keys` | מפתחות שנתרמו על ידי הציבור |
| `gemini_rr_index` | מונה סבב Round-Robin למפתחות |
| `key_exhausted:{shortKey}` | סטטוס חסימה למפתח ספציפי |
| `gemini_usage:{shortKey}` | מונה שימושים למפתח |
| `admin_session:{token}` | סשן ניהול אינטרנט (תוקף 24 שעות) |

---

## 🏥 שמירה על זמינות המערכת

### כישלון Redis
אם Redis לא זמין, המערכת עוברת לזיכרון זמני (in-memory). נתוני משתמשים לא יישמרו בין שיחות.

### כישלון Gemini
המערכת מנסה כל מפתח בסבב עד 3 פעמים לפני החזרת הודעת שגיאה.

### timeout Vercel
maxDuration מוגדר ל-60 שניות. שיחות MCP עלולות לקחת עד 25 שניות.

### גיבוי כישלון שרות מייל
אם שרות המייל (Google Apps Script) נכשל, המערכת מודיעה לאו המשתמש.

---

## 🛠️ תחזוקה ודיאגנוסטיקה

### לוגים ב-Vercel Runtime Logs
כל פעולה מתועדת עם prefix `[INFO]`, `[WARN]`, `[ERROR]`.

**חיפוש בעיות שכיחות:**
- `[GeminiAPI]` – בעיות מפתחות API
- `[UpstashRedis]` – בעיות Redis
- `[CodeAgentFlow]` – בעיות עויזר קוד
- `[TrebloFlow]` – בעיות יצירת שירים
- `[VoiceEngine]` – בעיות TTS נוירלי

### בדיקת סטטוס מפתחות
דרך שלוחת הניהול → **4** (סטטוס מפתחות API) – ניתן לראות בזמן אמת אילו מפתחות פעילים/חסומים ומתי ישוחררו.

---

## ⚠️ הערות אבטחה חשובות

1. **מספר הטלפון `adminBypassPhone`** המוגדר בקוד מאפשר כניסה לניהול ללא סיסמה – וודאו שזה מספר מאובטח.
2. **`ADMIN_PASSWORD`** – וודאו שמוגדר ב-Vercel ולא חשוף בקוד.
3. **ממשק ניהול האינטרנט** (`web=system_admin`) מוגן בסיסמה + cookie session – הסשן תקף 24 שעות.
4. **עויזר קוד** – מבצע שינויים על branch `dev` בלבד. מיזוג ל-`main` דורש אישור כפול דרך הטלפון.
5. **חסימת משתמשים** – משתמש חסום מקבל הודעה ומנותק מיד בתחילת כל שיחה.

---

*מדריך זה מיועד למנהלי המערכת בלבד. אין להפיצו.*
[/spoiler]
